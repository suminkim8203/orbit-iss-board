# 재개 전 점검

요약: 원본 제품 코드에 중단으로 인한 변경/손상은 없었다. 기존 데이터 검사 16개와 빌드 및 원본 fixture 무결성 검사를 통과했다. 이번 기능은 아직 미구현이다.

- 점검 시작: 2026-09-10T00:07:32Z (KST 09:07:32).
- 원본 버전: a5c034dea460468029e4618f45a47ac5a75bda2b.
- 어제 잘못 추정한 `app/src/App.tsx` 경로 조회는 파일 읽기 실패였으며 코드 변경은 없었다. 실제 화면 파일은 `app/app/page.tsx`이다.
- 기존 검사: 16/16 PASS. 빌드 PASS. 원본 17개 및 배포 fixture 9개 해시 대조 PASS. 비밀값 패턴 검사 발견 0건.
- 린트: 기존 node:test 등록 호출에서 반환 Promise를 명시적으로 무시하지 않아 실패했다. `void test(...)` 표기로 수정했고 검사 입력/기대값은 그대로 유지했다. 수정 후 lint PASS.
- 의존성: Node 24.19.0 / npm 11.17.0. npm ci 완료. 잠금 파일 기준 npm 감사는 11건(낮음 1, 중간 2, 높음 8)을 보고했다. 의존성 전체 업그레이드는 이번 기능 범위에서 수행하지 않았다. 에셋 보존을 위해 공개 fixture는 수정하지 않았다.
- 기존 잠재 오류: 공개 기록 0개에서는 current_reading 및 latestSaved.reading을 참조할 수 없다. 고정 F07로 확인할 대상이다.
- 시험 준비: Playwright 1.63.0을 정확한 버전으로 추가했다. 첫 시험 실행은 테스트 서버의 기준 폴더 지정 누락으로 시작되지 않았다. cwd를 명시하여 복구했다. 검사 판정 조건 변경은 없고, 이 단계는 공식 A 시작 전이다.
- Vite 개발 서버가 기존 public JSON import 경고를 출력하지만 화면은 렌더링된다. 브라우저 시험 서버의 데이터 주입도 실제 화면에서 확인했다.
- 고정 문서의 운영값 ‘미정’ 표기는 최초 문서 보존을 위해 그대로 두었다. 확정 운영 조건은 protocol.md를 따른다.
- 기존 공개 T04 사이트의 배포나 자동 수집 설정을 바꾸지 않았다.
